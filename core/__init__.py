"""Core, QGIS-independent algorithms for the ZTD-AITD plugin.

These modules contain no PyQGIS imports so they can be unit-tested with a plain
Python + NumPy environment. The QGIS-facing code lives in ``gui`` and
``workers``.
"""
